# certificate-service

FastAPI **required** service on **8002**. Printable HTML + public token route.

```bash
cp .env.example .env
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8002
```
